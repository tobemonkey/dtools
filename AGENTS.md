# AGENTS.md - dtools 个人工具平台协作规范

## 1. 项目定位

本仓库是一个个人工具集合平台，目标是在 macOS 和 Windows 上提供一致的桌面工具体验，同时保留 Web 化部署能力。

核心目标：

- 以工具协议为中心沉淀可复用能力，避免把单个工具写成孤立页面
- 前端负责工作台、交互和跨平台桌面壳；后端使用 Spring Boot 提供工具编排、历史记录和后续可扩展 API
- MySQL 8 作为可审计主账本，缓存、索引和本地桌面状态都视为可重建派生数据

## 2. 技术栈

- 后端：JDK 21、Spring Boot 3.5.x、Maven 多模块、MyBatis、MySQL 8
- 前端：Vue 3、TypeScript、Vite、Pinia
- 桌面：预留 Tauri 2 集成目录，后续用于 macOS / Windows 打包
- 代码风格：Java 21 语法，前端使用严格 TypeScript

## 3. 模块边界

- `dtools-common`：通用返回、异常、TraceID、基础工具
- `dtools-tools`：工具协议、工具注册、工具执行、历史主账本适配
- `dtools-bootstrap`：Spring Boot 启动入口、Controller、配置和运行时资源
- `frontend`：Web 工作台与后续 Tauri 前端入口
- `docs`：产品文档、技术文档、协议与数据库说明
- `plans/<任务名>`：任务分析、拆解、验收和后续风险

依赖方向：

- `bootstrap` 可以依赖 `tools / common`
- `tools` 可以依赖 `common`
- `common` 不依赖任何业务能力模块
- 前端只能通过公开 API 调用后端，不直接理解数据库结构
- 前端必须按 `api / types / stores / views / components / utils` 分层组织；页面级视图只负责组合布局和用户动作分发，API 请求、协议类型、跨组件状态和纯工具函数不得长期堆在单个页面文件中
- `src/api` 只封装后端公开接口调用，`src/types` 只定义前后端协议和前端领域类型，`src/stores` 使用 Pinia 管理跨组件状态和业务动作，`src/views` 存放页面级工作台，`src/components/<业务域>` 存放可复用业务组件，`src/utils` 存放无副作用纯函数
- 前端组件拆分应从用户路径和状态边界出发；中型工具页必须至少拆出 API 层、状态层、页面层和关键业务组件，避免把表单、接口调用、列表渲染、详情编辑和工具函数混在一个单文件组件中

## 4. 分支命名管理

- 开启新任务前必须先确认当前分支；如果当前任务与现有分支主题不一致，必须先创建或切换到独立任务分支，再开始修改代码、文档或配置
- 新功能分支：`feature/<功能>-<年月日>`，例如 `feature/new-260327`
- Bug 修复分支：`bugfix/<问题>-<年月日>`
- 冲突合并分支：`conflict/<来源>-<年月日>`
- 年月日使用 6 位短日期，格式为 `yyMMdd`
- 分支名必须使用英文、数字和中划线，避免中文和空格
- 未经用户明确要求，不得把新任务改动继续叠加在无关功能分支上

## 5. 命名与注释

- Java 类、接口、枚举、Controller 方法和公开 Service 方法必须写中文注释
- 作者统一署名：`yesterday'jam`
- 接口注释必须说明功能、需要注意的点、协议或调用方约束
- 属性无法直接见名知意，或可能造成理解冲突时，必须写中文注释
- 业务枚举必须遵循统一规范：至少包含稳定数字 `code` 和中文说明 `desc` 两个字段
- 枚举的 `code` 是数据库存储、前后端交互、日志审计和外部协议中的唯一稳定值，禁止把 `enum.name()` 直接写入数据库或作为接口协议值
- 枚举必须提供通过 `code` 反查枚举的方法；找不到时应返回明确业务错误或受控默认值，不允许静默吞掉未知 code
- Java `record` 的每个组件字段必须逐项补充注释；不适合补充注释时优先使用普通类

注释模板：

```java
/**
 * @description: 工具执行入口，负责参数校验和调用编排，不直接处理数据库细节
 * @author: yesterday'jam
 * @date: 2026/05/27
 * @注意: 新增工具时必须先补充 ToolDefinition，再补充执行器与 Mock 验证
 */
```

## 6. 开发原则

- 协议先行：先定义 API Contract、DTO、数据库字段，再写实现
- 响应码收敛：`ApiResponse.code` 必须使用 `Integer` 类型，并由统一响应码枚举维护，响应码枚举同样必须包含 `code` 和 `desc`
- 响应码治理：禁止在业务代码中随意新增字符串 code；新增响应码必须先进入统一枚举并写清含义、适用场景和调用方处理建议，避免 code 膨胀
- 工具化思维：每个个人工具都必须能被 `ToolDefinition` 描述
- 主账本优先：工具执行历史、用户配置和重要状态优先进入 MySQL 8
- 可回放：工具执行输入、输出摘要、耗时和 TraceID 必须可追踪
- 兼容优先：新增工具不能破坏已有工具协议
- 小步验证：每次变更至少运行相关编译、类型检查或 Mock 测试
- 渐进式建模：产品规则和长期方向可以提前在文档中分析，但数据库表、字段、接口和代码实现必须跟随当前功能阶段逐步落地；未进入当前实现阶段的表和字段只能放入“候选设计 / 后续扩展 / 待确认项”，不得提前写入正式 schema 或代码骨架
- 最小可验收切片：每个阶段只创建本阶段验收所需的表、字段、DTO、接口和页面；例如基础 CRUD 阶段不提前落地提醒、承接、快照等后续阶段专用表
- Service 用例优先：新增业务能力时，先从用户动作和应用服务接口出发定义 `Command / Query / Config` 等 Service 能力，再由 Service 用例倒推出 Mapper SQL、DTO 和 Controller；不得先按表铺通用 CRUD 再反向拼业务
- Service 命名分层：`XxxService` 表示对 Controller 或其他模块暴露的应用服务接口；`XxxServiceImpl` 必须放在 `service/impl` 包下，负责实体或资源级基础能力，例如基础新增、修改、查询、取消、排序和简单事务边界；`XxxBizService` 放在 `service/biz` 包下，负责该实体支持的业务规则、跨表编排、状态判断、批量策略和可复用业务逻辑，不直接作为 Controller 门面
- BizService 抽取原则：`BizService` 应围绕实体或业务域的能力聚合，例如 `TodoBizService`、`TodoTagBizService`、`TodoBlockBizService`，不得按 `Create / Update / Delete` 这类单一动作拆成大量碎片服务；当业务逻辑超过基础 CRUD、跨多个 Mapper 或需要独立测试时，再从 `ServiceImpl` 下沉到 `BizService`
- ServiceImpl 与 BizService 边界：`ServiceImpl` 负责接收应用用例、控制事务、调用 BizService 和组装 DTO；业务判断和规则实现优先放在 BizService，避免 Controller 或 ServiceImpl 堆积复杂规则，也避免 BizService 退化为只有一行 Mapper 调用的空壳
- Mapper 按用例设计：Mapper 方法必须服务当前 Service 用例，优先表达具体查询、批量写入、条件更新或状态变更意图；避免为每张表预置 `insert / findById / update / delete` 这类通用方法，除非当前用例明确需要
- 谨慎复用现成方法：已有 Mapper、Service 或工具方法不代表适合新用例；使用前必须确认它的查询条件、事务边界、锁语义、批量能力、返回字段和业务约束与当前用例一致，不一致时应新增面向当前用例的方法
- 前后端纵向切片：涉及前端交互和后端协议的功能，默认按用户路径做前后端闭环切片；每个切片都应包含必要的后端接口、前端入口或交互、联调验证和截图，不得长期只推进后端或只推进前端导致上下文偏移
- Subagent 协作：复杂功能默认由主 agent 统一维护产品上下文、任务拆分、接口契约和验收标准；后端实现、前端实现、文档/图示可以交给不同 subagent 并行推进，但主 agent 必须负责发放任务、合并结果、检查前后端一致性和最终验收
- 主 agent 职责：主 agent 不能把上下文同步责任交给 subagent；在每个切片开始前必须明确本切片用户路径、接口契约、数据模型边界、验收命令和截图要求，在切片结束后统一更新 `plans/`、`docs/` 和交付说明
- 前端分层优先：新增或重构前端能力时，先确认页面视图、Pinia store、API 封装、类型定义和组件拆分边界，再写具体交互；不得为了快速联调把长期代码集中写入 `App.vue` 或单个大型页面文件
- 前端状态边界：跨组件共享、需要后端同步或会影响多个区域展示的状态必须进入 Pinia store；局部输入态、临时展开态和单组件 UI 态可保留在组件内部

## 7. AI 协作规范

本项目参考 GitHub Copilot repository instructions、AGENTS.md 社区实践和 Codex 项目指令模式，采用“根规范 + 任务计划 + 验收命令”的轻量结构：

- 根目录 `AGENTS.md` 是所有 AI Coding Agent 的最高项目规范
- `.github/copilot-instructions.md` 作为 GitHub Copilot 的镜像入口，内容指向本文件和关键命令
- 复杂任务必须创建 `plans/<任务名>/task.md`、`analysis.md`、`todo.md`
- 不把大量背景知识塞进单个提示；长期设计沉淀到 `docs/`
- 每个任务文档必须包含验收命令，避免“按最佳实践”这类不可验证要求
- 每个中型任务必须评估是否需要独立技术文档；只要涉及新表设计、关键状态机、复杂业务规则、跨模块数据流或前后端交互契约，就必须创建或更新对应技术文档
- 中型任务如果包含 3 个以上组件交互、状态流转、定时流程、承接流程或复杂决策树，必须生成或更新 drawio 流程图；简单 CRUD 或纯样式调整可省略
- Git 提交信息必须使用中文，清楚描述本次小任务完成的具体内容
- 提交粒度必须按“功能拆分细节”提交，例如协议/DTO、执行器、Controller、前端表单、前端结果展示、文档更新分别提交；不得等到整个功能完成后一次性提交大批代码，避免 CR 困难
- 每个小提交应只包含同一目的的文件改动；如果同一轮工作同时出现规范、后端、前端、文档改动，应拆分为多个提交

## 7.1 阶段隔离与多会话协作

复杂工具 App 默认采用“阶段内共享上下文，阶段间强隔离”的协作模式。Agent 效果提升不只来自 Agent 数量增加，更来自上下文隔离增加；阶段间只传递冻结产物，避免确认偏差、历史惯性和错误传播。

阶段规则：

- 产品探索阶段：允许单会话多角色讨论，用户、PM Agent、Architect Agent 可以共享上下文；目标是产出冻结版 PRD、交互规则和必要 UI 图。
- 架构设计阶段：必须进入新会话或新工作上下文；输入只包含冻结 PRD、根规则和必要约束；输出冻结版 architecture / technical design。
- 编码实现阶段：必须进入新会话或新工作上下文；输入只包含冻结 PRD、冻结架构、任务计划和必要规则；输出代码、测试和实现说明。
- 攻击测试 / QA 阶段：必须进入新会话或新工作上下文；默认只输入代码、测试入口和运行方式，不输入产品探索或架构讨论过程；目标是质疑实现、寻找 bug、边界问题、数据一致性问题、安全风险和交互缺陷。

阶段冻结要求：

- 每个阶段结束必须产生可落盘产物，例如 PRD、architecture、plan、代码、测试报告、截图、攻击测试报告。
- 不能把“上下文中已经完成”视为交付；subagent 或新会话的输出必须落到文件、截图、测试结果或明确报告。
- 下一阶段只读取上一阶段冻结产物，不读取上一阶段完整聊天过程；如冻结产物缺失关键信息，必须回到上一阶段补文档，而不是凭聊天记忆继续。

切换提示要求：

- 当某阶段已结束，并且按照本规则下一步应进入新会话或新工作上下文时，主 Agent 必须明确提示用户“建议切换到新会话”，并说明新会话应携带哪些冻结产物。
- 提示必须包含：当前阶段已完成的产物、下一阶段名称、新会话输入清单、不要携带的上下文类型。
- 用户确认继续留在当前会话时，可以继续，但交付说明中必须记录这是用户确认的例外。

## 8. 自主边界与用户确认

- AI 可以提出产品、工具、样式、架构和规范建议，但不得在未获得用户明确确认前实现超出原始诉求的功能。
- “搭建工具集合 App”只代表需要搭建平台能力，不代表可以自行决定具体工具清单；具体工具必须先列出候选项，等用户确认后再创建。
- “前端部分你处理”只代表可以实现前端工程与交互，不代表可以自行决定最终视觉风格；涉及明显审美方向、设计系统、主题色、布局范式时，必须先给出样式样例或方案让用户选择。
- 主动想法必须以“建议 / 可选方案 / 待确认项”的形式呈现，不得静默落地成代码、文档或配置。
- 如果为了验证平台必须创建最小示例，必须使用明确标注的 `demo` 或 `example`，并在交付说明中提示可删除；不能把示例伪装成用户真实需求。
- 当用户指出 AI 擅自扩展范围时，后续处理优先级为：先记录规范，再征求确认，再清理越界实现。

## 9. 复杂任务文档要求

复杂任务需要先做整体分析设计，并创建：

- 产品文档：用户目标、核心场景、信息架构、交互路径、验收标准
- 技术文档：模块边界、数据流、API、数据库表、设计模式、采用理由、风险
- 任务计划：任务拆解、产出物、验证方式和当前状态

## 10. 默认验证命令

- 后端编译：`mvn -q -DskipTests compile`
- 后端测试：`mvn -q test`
- 前端类型检查：`npm --prefix frontend run typecheck`
- 前端构建：`npm --prefix frontend run build`

## 11. 交付标准

- 代码、文档和计划同步更新
- 新增工具必须能在前端目录中看到入口，在后端目录中看到协议和执行器
- 新增页面或更新 UI 时，默认生成一张图片或截图作为展示说明；改动非常简单或用户明确不需要时可省略
- 复杂逻辑必须带 TraceID
- 交付说明必须写明验证结果与未完成风险
